#include <stdio.h>

int main() {
    float a;
    
    // Read a float value
    scanf("%f", &a);

    // Print the float value with 4 decimal places
    printf("You entered: %.4f", a);

    return 0;
}
